name: Deploy to Firebase Hosting
'on':
  push:
    branches:
      - firebase-hosting
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_ECOMMERCEWEBSITE_262F6 }}'
          channelId: live
          projectId: ecommercewebsite-262f6
